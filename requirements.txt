# Pinned to versions verified to work together (Python 3.12).
# aioinflux is unmaintained (last release 2019) and relies on aiohttp's
# deprecated `loop=` argument, so aiohttp is pinned deliberately: a newer
# aiohttp that removes `loop=` would break the InfluxDB write path.
aioinflux==0.9.0
aiohttp==3.14.3
ciso8601==2.3.3
pyModbusTCP==0.3.0
